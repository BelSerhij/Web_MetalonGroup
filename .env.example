# Generate a high-entropy value, for example: openssl rand -base64 48
AUTH_SECRET=

# Required only when initializing a development database with `npx prisma db seed`.
INITIAL_ADMIN_PASSWORD=

# Used only by `npm run admin:reset`; it never deletes CRM data.
ADMIN_EMAIL=admin@metalon.ua
ADMIN_PASSWORD=AdminMetalon123

DATABASE_URL=
DIRECT_URL=
